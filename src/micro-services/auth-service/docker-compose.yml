version: "2"
services:
  authservice:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
      - $HOME/logs:/var/log
    depends_on:
      - db
    links:
      - db
    ports:
      - "3001:3000"
    environment:
      - INSTANCE=authservice
      - AUTH_SERVICE_BASEPATH=${ENV_AUTH_SERVICE_BASEPATH}
      - CONSENT_SERVICE_BASEPATH=${ENV_CONSENT_SERVICE_BASEPATH}
      - DB_USERNAME=${ENV_DB_USERNAME}
      - DB_PASSWORD=${ENV_DB_PASSWORD}
      - DB_NAME=${ENV_DB_NAME}
      - DB_HOST=${ENV_DB_HOST}
      - DB_PORT=${ENV_DB_PORT}
      - SMS_SERVICE_ACTIVE=false
      - SMS_API_ENDPOINT=${ENV_SMS_API_ENDPOINT}
      - SMS_PASSPHRASE=${ENV_SMS_PASSPHRASE}
      - SMS_APP_ID=${ENV_SMS_APP_ID}
      - SMS_APP_SECRET=${ENV_SMS_APP_SECRET}
    networks:
      - authservice_default
  db:
    restart: always
    image: postgres:10
    environment:
      - POSTGRES_USER=wakanda_auth_service
      - POSTGRES_PASSWORD=12345
      - POSTGRES_DB=wakanda_auth_db

    volumes:
      - ./postgres-data/postgres:/var/lib/postgresql/data
      - ./dbdata:/docker-entrypoint-initdb.d/
    ports:
      - "5433:5432"
    expose:
      - "5432"
    networks:
      - authservice_default

networks:
  authservice_default:
    driver: bridge
