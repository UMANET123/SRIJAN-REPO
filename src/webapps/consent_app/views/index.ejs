<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Globe consent</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>
    <% include ./_partials/header %>
    <main>
      <div class="login-form-container container">
        <div class="login-form-image"></div>
        <div class="login-form">
          <h3>Globe Login</h3>
          <div class="form-fields">
            <div id="postResultDiv"></div>
            <div class="form-group">
              <label for="Phoneno">Phone No:</label>
              <input
                type="text"
                class="form-control"
                id="phone_no"
                placeholder="Phone No"
              />
              <input
                type="hidden"
                class="form-control"
                id="transaction_id"
                value="<%= transaction_id %>"
              />
              <small id="emailHelp" class="form-text text-muted helper-text"
                >Please enter Globe no starting with 63. e.g -
                639953xxxxxx.</small
              >
              <input
                type="hidden"
                class="form-control"
                class="generate_otp"
                id="subscriber_id"
                placeholder="Subscriber Id"
              />
            </div>
            <div class="form-group" id="otp_form_group" style="display:none">
              <label for="otp">OTP:</label>
              <input
                type="text"
                class="form-control"
                id="otp"
                placeholder="OTP"
              />
            </div>
            <div class="login-actions">
              <div class="generate-otp">
                <a href="#" class="generate_otp" id="generate_otp"
                  >Generate OTP</a
                >
              </div>
              <button
                type="submit"
                class="btn btn-default"
                id="verify_otp"
                style="display:none"
              >
                Login
              </button>
            </div>
          </div>
        </div>
      </div>
      <% include ./_partials/footer-menu %>
    </main>
  </body>
  <script src="/js/login.js"></script>
</html>
